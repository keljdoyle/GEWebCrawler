The project consists of a command line app that will parse JSON files in the resources folder.
It parses pages recursively and keeps track of link states in WebCrawler class.
For simplicity the Java 8 parallel collections streams are used for link processing.
